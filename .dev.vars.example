# Opcional: sem esta chave, o consultor usa a análise financeira local.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini
# openai requires an API key and available API credits. ChatGPT plans are separate.
AI_PROVIDER=openai
