name: Publicar na Cloudflare

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      HAS_CLOUDFLARE_SECRETS: ${{ secrets.CLOUDFLARE_API_TOKEN != '' && secrets.CLOUDFLARE_ACCOUNT_ID != '' }}

    steps:
      - name: Baixar o código
        uses: actions/checkout@v4

      - name: Preparar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Instalar dependências
        run: npm ci

      - name: Gerar tipos dos bindings
        run: npm run cf:types

      - name: Validar tipos e lint
        run: npm run typecheck && npm run lint

      - name: Gerar aplicação
        run: npm run build:cloudflare

      - name: Validar testes e artefato
        run: node --test tests/*.test.mjs

      - name: Aplicar estrutura do banco
        if: env.HAS_CLOUDFLARE_SECRETS == 'true'
        run: npx wrangler d1 migrations apply DB --remote
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Publicar Worker
        if: env.HAS_CLOUDFLARE_SECRETS == 'true'
        run: npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Informar ausência de publicação
        if: env.HAS_CLOUDFLARE_SECRETS != 'true'
        run: echo "Validação concluída. Este workflow não publicou porque os secrets Cloudflare não estão configurados."
